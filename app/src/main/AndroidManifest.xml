<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="syfy.co.za.journal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity
        android:name="syfy.co.za.journal.ui.JournalEntries.JournalEntriesActivity"
        android:theme="@style/AppTheme.OverlapSystemBar">
    </activity>

    <activity android:name="syfy.co.za.journal.ui.Authentication.AuthenticationActivity"> <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
    </intent-filter>
        </activity>

    <activity android:name="syfy.co.za.journal.ui.EntryDetail.EntryDetailActivity" />

    <activity android:name="syfy.co.za.journal.ui.AddEditEntry.AddEditEntryActivity" />

    </application>

    </manifest>
